# P08 Try-On Visualization

Status: ACCEPTED FOR PERSONAL MVP WITH LIVE TRY-ON GATES DEFERRED

Accepted run:

- Try-on visualization: `20260715T095103Z-6fcfb33f`

| Requirement | Status | Result |
|---|---|---|
| `P08-EXP-001` | PASS | Explicit preview and single-use submission create a durable asynchronous try-on job |
| `P08-LBL-001` | PASS | Completed images carry the exact AI visualization disclaimer |
| `P08-SRC-001` | PASS | Results retain the saved outfit, ordered item IDs, and verified real garment sources |
| `P08-EVD-001` | PASS | Generated images remain presentation-only and deletion previews include their complete local closure |
| `P08-ERR-001` | PASS | The fixed image-edits transport and durable queue fail closed with typed outcomes |
| `P08-PRV-001` | PASS | Provider, purpose, retention terms, portrait, and garments are disclosed before the only submission action |
| `P08-QLT-001` | DEFERRED | No blinded live identity and garment-detail quality study is available |

The accepted local vertical selects a previously analyzed photo, snapshots its
verified immutable source bytes with two to eight pinned outfit garments, and
discloses the exact transmitted references before submission. Inputs are
deterministically canonicalized to bounded metadata-free PNGs in memory; no
second durable input copy is created.

The production path uses the fixed OpenAI `gpt-image-2` image-edits endpoint
behind exact `experimental` build gates. Keychain lookup occurs only after a
fenced database transaction confirms active credential authority. Jobs survive
restart, dispatched-but-unknown work is never resent automatically, promoted
outputs recover atomically, and generated images remain labeled,
presentation-only, and ineligible as wardrobe evidence.

Deletion previews close over approvals, asset snapshots, jobs, attempts,
command receipts, materialization intents, outputs, provenance, remote
retention references, and every schema-level blob owner. Provider retention is
reported separately from local records, and references are shown only for
attempts that crossed the transport boundary.

The phase boundary passed the packaged desktop build, full Rust, Python, and
React regressions, migration backup/restore checks, concrete local TLS
transport tests, deletion schema classification, accessibility, and one
Playwright try-on smoke across desktop and mobile.

Live OpenAI interoperability, the blinded quality study, notarization, and
clean-machine certification remain `deferred_not_passed`. Missing credentials
perform zero production calls, and the feature remains experimental.
